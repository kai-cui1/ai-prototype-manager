- Schema 复用机制：将通用的字段约束（如 UUID 格式、名称正则、分页参数）抽取至 `base.ts`，在各业务 Schema 中通过展开运算符或引用方式复用。
- PRD 规则映射：在 Schema 文件头部及字段注释中明确标注对应的 PRD 业务规则编号（如 G-M1-04、B-M1-88），实现代码与需求文档的双向追溯。
- 列表与详情分离：针对同一业务实体（如 Project），分别定义精简的 `ListItem` Schema（含内嵌统计摘要）和完整的 `Detail` Schema，以优化网络传输效率。