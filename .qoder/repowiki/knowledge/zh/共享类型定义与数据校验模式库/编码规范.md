- 业务实体名称字段遵循严格的正则约束（如字母开头、允许下划线/连字符），并在 Create 与 Update 场景中可能采用不同严格度的 Schema。
- 所有列表查询接口均复用标准的 PaginationQuery 组合模式，包含 page、pageSize、search 及可选的 sort/order 字段。
- API 响应体必须通过 SuccessEnvelope 或 PaginatedEnvelope 进行包装，禁止直接返回原始数据对象，以统一前端处理逻辑。
- 涉及并发更新的资源操作必须在 Schema 中显式包含 version 字段，以支持乐观锁机制并防止数据覆盖。