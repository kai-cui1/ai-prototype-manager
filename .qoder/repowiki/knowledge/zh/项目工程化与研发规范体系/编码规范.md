- PRD 采用纯业务层定位，业务流程图使用 Mermaid 且泳道限定为用户/AI/系统，严禁出现页面级交互细节。
- 编码实施遵循 C1-C5 循环：Service 层实现业务逻辑 → Route 层适配 HTTP → 前端实现交互 → TDD 测试验证 → Code Review 质量门禁。
- 测试数据强制安全隔离，所有自动化测试产生的 name 字段必须以 'e2e-' 为前缀，清理操作必须带 WHERE 过滤以防止误删生产数据。
- API 测试用例编号遵循 TC-API-{MODULE}-{FEATURE}-{SEQ} 格式，每个功能点文件末尾必须包含覆盖矩阵以回溯 PRD 验收标准。
- 代码注释执行 R1-R5 强制规则，要求每个导出函数具备 JSDoc，关键分支与非显而易见逻辑必须包含意图说明与原因解释。