- 所有项目级页面组件必须包裹在 <ProjectRouteGuard> 中以验证项目上下文有效性。
- 采用 Context API (ProjectContext, DomainModelContext 等) 作为跨组件状态共享的核心机制，替代全局状态库。
- 页面级组件位于 src/pages/，复杂交互逻辑拆分为 src/components/ 下的领域特定目录（如 domain-model, process-editor）。