- 统一编号体系：功能点(F-)、业务规则(B-)、全局规则(G-)、验收标准(AC-)及测试用例(TC-API-)均采用模块化前缀加两位序号的格式，确保跨文档引用的一致性。
- 测试数据隔离铁律：所有自动化测试产生的数据实体名称必须带有`e2e-`前缀，且清理操作必须通过带前缀过滤的WHERE条件执行，严禁无条件全表删除。
- 注释强制规范（R1-R5）：每个导出函数必须包含JSDoc，每个分支逻辑需有意图注释，长代码段需有段落概括，关键业务逻辑需解释“为什么”而非仅描述“是什么”。
- PRD纯业务层定位：产品需求文档严禁包含页面布局、UI元素清单等交互细节，此类内容必须剥离至独立的S3交互设计文档，保持业务规格的纯粹性。