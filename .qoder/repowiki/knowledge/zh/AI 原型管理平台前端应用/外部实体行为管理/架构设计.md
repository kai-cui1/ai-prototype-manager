- 采用页面与逻辑分离架构，`ExternalEntitiesPage` 和 `ExternalEntityDetailPage` 负责UI渲染与路由参数处理。
- `useExternalEntityBehavior` 作为核心自定义Hook，封装了针对 Actions 和 Decisions 的API调用、状态管理及乐观锁版本控制。
- 详情页通过 Tab 切换实现行为与决策视图的隔离，并集成 `ActionFormDialog` 与 `DecisionFormDialog` 处理复杂的表单交互。
- 依赖 `@/lib/api-client` 进行RESTful通信，并通过 `react-router-dom` 管理项目上下文下的资源导航。