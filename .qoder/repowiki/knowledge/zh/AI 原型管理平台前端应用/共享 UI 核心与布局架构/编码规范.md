- 所有 UI 组件均通过 `cn()` 函数合并 Tailwind 类名，支持外部 `className` 覆盖默认样式。
- API 请求统一通过 `apiClient` 实例发起，错误处理在拦截器中统一提取 `error.message`。
- 复杂页面容器（如 `DataTableContainer`）采用组合模式，将加载、空状态和内容展示逻辑封装在单一容器中。
- 自定义 Hook（如 `useDebouncedValue`）遵循受控状态模式，通过 `useEffect` 延迟更新派生状态。