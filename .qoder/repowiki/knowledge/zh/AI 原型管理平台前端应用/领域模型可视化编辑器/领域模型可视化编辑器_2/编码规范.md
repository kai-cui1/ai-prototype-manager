- 所有自定义节点与边组件均使用 `React.memo` 进行包裹，以优化大规模图谱渲染时的性能。
- 涉及删除或关键变更的操作（如删除实体、关系）均通过 `AlertDialog` 二次确认组件执行，防止误操作。
- 表单输入项在失去焦点（onBlur）或特定选择变更时自动触发保存逻辑，提供无感知的持久化体验。
- 实体与关系的标识符（Name/Key）遵循严格的正则校验（字母开头，仅含字母数字下划线），确保后端兼容性。