- 所有删除操作均采用 `AlertDialog` 组件实现二次确认，并在执行异步删除时禁用操作按钮以防止误触。
- 标识符字段（如实体的 `name`、字段的 `name`）在创建时强制执行正则校验 `/^[a-zA-Z][a-zA-Z0-9_]*$/`，确保符合编程命名规范。
- 表单提交逻辑统一包含 `submitting` 状态管理，在异步请求期间禁用提交按钮并显示加载提示（如“创建中...”）。
- 新建与编辑场景复用同一对话框组件（如 `FieldDialog`），通过传入的数据对象是否为 `null` 来动态切换 UI 表现（如只读显示名称）与 API 调用逻辑。