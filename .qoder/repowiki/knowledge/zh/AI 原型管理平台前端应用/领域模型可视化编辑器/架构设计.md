- **状态中枢**：`DomainModelContext` 作为唯一状态源，管理实体/关系/领域的选中互斥逻辑、视图模式及画布配置，并通过 `useDomainModel` Hook 封装底层 API 调用。
- **数据驱动渲染**：所有子组件（Canvas, Inspector, Dialogs）均通过 `useDomainModelContext` 消费状态，确保在图形视图与列表视图切换时数据一致性。
- **交互闭环**：对话框组件执行 CRUD 操作后，自动触发 Context 中的刷新机制（如 `refetchGraph`），实现画布与属性面板的实时联动更新。