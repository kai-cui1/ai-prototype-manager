- 所有写操作（创建/更新/删除）成功后必须触发对应列表或详情的自动刷新（refetch）。
- 针对 409 冲突错误，需在 UI 层面转换为字段级错误提示并保持对话框打开，而非直接关闭。
- 当项目处于 `archived` 状态时，全局隐藏所有新建、编辑、删除等变更类操作按钮。
- 标识符（name）字段的校验逻辑需根据业务场景（创建 vs 编辑）动态切换正则表达式规则。
- 列表页搜索框必须配合防抖 Hook（useDebouncedValue）使用，避免频繁触发后端查询。