- 服务层函数采用纯函数风格，将数据库实例 `db` 作为第一个参数显式传入，避免依赖全局单例状态。
- 路由层统一使用 TypeBox Schema 定义请求体、查询参数及响应结构，并自动注入 OpenAPI 文档标签与描述。
- 业务逻辑中广泛使用乐观锁机制（version 字段），在更新或归档操作前校验版本号以处理并发冲突。
- 数据库查询大量使用关联子查询或 Promise.all 并行执行，以减少网络往返次数（RTT）并避免 N+1 问题。