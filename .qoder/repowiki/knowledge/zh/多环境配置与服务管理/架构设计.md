- 配置层：以 `*.json`（如 `dev1.json`）定义各环境的端口、URL 及数据库元数据，`.active` 记录当前选中环境。
- 交互层：`set-env.sh` 解析 JSON 并 export 环境变量（`DATABASE_URL` 等），强制要求 `source` 执行以确保变量注入当前 Shell。
- 运维层：`restart-env.sh` 编排服务生命周期，先调用 `set-env.sh` 激活环境，再利用 `lsof` 清理端口占用并启动 `pnpm dev`。
- 约束机制：明确 AI 仅可读配置与验证连通性，严禁修改文件或操作服务，确保环境系统的稳定性。