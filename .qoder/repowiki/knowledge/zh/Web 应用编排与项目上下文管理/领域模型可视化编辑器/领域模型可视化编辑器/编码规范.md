- 所有数据变更操作（增删改）在 Context 中均包装为异步函数，执行成功后自动触发关联列表或图谱的重新拉取以保持 UI 同步。
- 实体、关系与领域边界的选中状态在 Context 中保持互斥逻辑，选中任一对象时会自动清除其他类型的选中态。
- UI 组件统一通过自定义 Hook `useDomainModelContext` 访问业务状态与操作方法，避免直接依赖底层 Context 对象。