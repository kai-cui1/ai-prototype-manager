- 采用 `DomainModelContext` 作为核心状态总线，封装 `useDomainModel` Hook 提供的 API 调用与自动刷新逻辑。
- `DomainModelPage` 负责注入 `projectId` 并挂载 Provider，确立数据作用域。
- `DomainModelEditor` 根据上下文中的 `viewMode` 动态切换 `ERCanvas`（React Flow）与 `EntityListView` 渲染。
- 所有子组件（画布节点、检查器面板、操作对话框）均通过 `useDomainModelContext` 消费统一的状态与操作方法，实现选中互斥（实体/关系/领域）及变更后的视图自动更新。