- 所有对话框组件均遵循受控模式，通过 `open` 属性控制显示，并通过 `onOpenChange` 回调通知父组件状态变更。
- 表单提交时统一使用 `submitting`/`deleting` 状态锁定提交按钮并显示加载文本，防止重复提交。
- 标识符字段（如实体名、字段名）强制要求以字母开头且仅包含字母、数字和下划线，并在输入时实时校验。
- 危险操作（删除）对话框统一使用 `AlertDialog` 组件，并在描述中明确告知用户操作不可撤销及潜在影响。
- 新建与编辑模式共用同一对话框组件时，通过判断传入的资源对象是否为 `null` 来动态切换标题、按钮文本及表单行为。