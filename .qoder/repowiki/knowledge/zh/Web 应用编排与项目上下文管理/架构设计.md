- **双层路由架构**：在 `App.tsx` 中定义全局层（Dashboard）与项目上下文层（`/p/:projectId/*`），后者统一包裹 `ProjectRouteGuard` 进行权限与状态校验。
- **全局状态注入**：在 `main.tsx` 顶层挂载 `ProjectProvider`，确保所有子模块均可访问当前激活的项目 ID 及元数据，实现跨页面的上下文一致性。
- **模块化视图组装**：各业务子模块（如领域模型、流程编辑器）作为独立页面组件接入路由，依赖统一的 `Layout` 和 `ErrorBoundary` 提供一致的交互框架与异常处理。