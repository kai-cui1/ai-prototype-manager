- 统一使用 `apiClient` 进行网络请求，并在 Hooks 层通过 `useState` 和 `useCallback` 管理加载状态与数据刷新逻辑。
- 表单提交前执行统一的标识符正则校验（`^[a-zA-Z0-9_-]{2,50}$`），并在 UI 层通过 `errors` 状态对象实时反馈字段级错误。
- 采用“列表页 + 弹窗（Dialog/AlertDialog）”的交互模式处理实体的新建、编辑与删除操作，保持主页面布局的稳定性。
- 在列表筛选场景中普遍应用 `useDebouncedValue` Hook 实现 300ms 的搜索防抖，以优化高频输入下的接口请求性能。