- 所有 UI 组件必须通过 `cn()` 函数合并类名，以支持 Tailwind 冲突解决和条件样式。
- 业务逻辑与数据获取通过自定义 Hooks（如 `useProjectContext`, `useDebouncedValue`）或 Context Provider 进行封装，避免在视图层直接处理副作用。
- API 请求统一使用 `lib/api-client.ts` 中配置的 Axios 实例，并通过拦截器统一处理错误消息提取。
- 视觉样式严格遵循 `lib/design-tokens.ts` 中定义的 Design Token 体系，禁止在组件中硬编码颜色或间距值。