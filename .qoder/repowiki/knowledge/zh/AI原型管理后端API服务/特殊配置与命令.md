- **数据库迁移**：通过 `npm run db:generate` 根据 schema 生成迁移文件，`npm run db:migrate` 执行迁移。
- **环境依赖**：启动前必须设置 `DATABASE_URL` 环境变量，否则 `src/db.ts` 会抛出错误并提示使用 `environments/set-env.sh` 脚本激活环境。
- **开发启动**：使用 `npm run dev` (tsx watch) 启动热重载服务，默认监听 13180 端口。