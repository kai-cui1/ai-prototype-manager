- **环境依赖**: 必须设置 `DATABASE_URL` 环境变量，通常通过 `source environments/set-env.sh` 激活特定环境配置。
- **数据库迁移**: 使用 `npm run db:generate` 根据 `src/models/schema.ts` 生成迁移文件，`npm run db:migrate` 执行迁移。
- **开发启动**: `npm run dev` 使用 `tsx watch` 监听源码变化并重启服务，默认端口 13180。