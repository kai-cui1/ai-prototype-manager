- 通过 `pnpm-workspace.yaml` 定义 `packages/*` 物理边界，实现依赖隔离与共享库（shared_libs）的本地链接。
- 利用 `turbo.json` 声明式任务图（Task Graph），强制执行 `build` 依赖顺序（`dependsOn: ["^build"]`）并管理跨模块环境变量注入。
- 根目录 `package.json` 提供统一的 CLI 入口（如 `pnpm dev`），将并发执行权委托给 Turbo 引擎，确保全栈服务（Web/API/DB）的协同启动与状态同步。