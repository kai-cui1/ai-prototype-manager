- 通过 `pnpm-workspace.yaml` 声明 `packages/*` 为子模块工作区，实现依赖提升与本地链接。
- 利用 `turbo.json` 定义任务拓扑（如 `build` 依赖 `^build`），确保共享库优先编译及服务间启动顺序。
- 在根级 `package.json` 提供统一的 `dev`、`build` 及 `test:e2e` 脚本入口，屏蔽子模块底层实现差异。