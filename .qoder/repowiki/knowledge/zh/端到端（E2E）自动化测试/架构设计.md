- **分层结构**：`tests/` 存放按业务模块划分的 Spec 文件，`helpers/` 提供 Page Object 封装、API 客户端及数据清理工具。
- **执行策略**：采用串行执行模式以避免数据库并发冲突，配置为失败时自动捕获截图与 Trace 日志。
- **交互边界**：通过 `page.route` 拦截网络请求实现 API Mock，确保 UI 测试不依赖后端实时数据状态；同时提供直连 API 的辅助工具用于前置数据准备。
- **环境约束**：明确禁止在配置中自动启停服务，要求测试前由用户手动声明并启动基础设施。