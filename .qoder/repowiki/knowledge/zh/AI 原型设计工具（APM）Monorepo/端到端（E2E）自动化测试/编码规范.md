- 采用页面对象模型（Page Object Model），将页面元素定位与交互逻辑封装在 `helpers/page-objects.ts` 中，测试用例仅调用语义化方法。
- 实施数据隔离策略，所有测试生成的数据均使用统一前缀（如 `e2e-`），并在测试结束后通过 API 调用进行归档或清理。
- 在 UI 交互测试中广泛使用 `page.route` 拦截并 Mock API 响应，以解耦前端渲染验证与后端业务逻辑。