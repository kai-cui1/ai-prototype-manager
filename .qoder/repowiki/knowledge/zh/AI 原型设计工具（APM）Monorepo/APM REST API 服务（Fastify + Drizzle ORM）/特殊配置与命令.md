启动需设置 `DATABASE_URL` 环境变量（提示脚本 `source environments/set-env.sh [env]` 或 `./environments/restart-env.sh dev1`）；开发命令 `pnpm dev` 通过 `tsx watch src/app.ts` 热重载；数据库迁移 `pnpm db:migrate` / `db:generate` / `db:studio` 基于 `drizzle/config.ts`；测试在 `VITEST=1` 模式下通过 `app.inject()` 发起请求而不监听真实端口。