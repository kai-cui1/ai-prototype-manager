根级脚本：`pnpm dev` 通过 turbo run dev 同时拉起 web/api/mcp 开发服务；`pnpm test:e2e` 执行 Playwright 端到端套件；`environments/set-env.sh <env>` 选择 dev1/dev2/uat 并 export DATABASE_URL，随后用 `environments/restart-env.sh` 重启受影响服务。