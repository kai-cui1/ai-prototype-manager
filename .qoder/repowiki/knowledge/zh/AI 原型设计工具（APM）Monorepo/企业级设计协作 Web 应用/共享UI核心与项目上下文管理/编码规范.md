- 所有UI组件均通过 `cn()` 工具函数合并 Tailwind CSS 类名，以确保样式冲突时的正确覆盖与合并。
- API 客户端统一使用 `apiClient` 实例，并在拦截器中标准化错误消息提取逻辑，避免在各处重复处理 HTTP 错误。
- 复杂业务组件（如 DataTableContainer）采用组合模式，通过 `children` 属性接收表格内容与分页控件，保持容器组件的通用性与灵活性。