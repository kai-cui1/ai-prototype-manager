- CRUD 操作成功后自动触发列表刷新（如 `refetchRoles`），确保 UI 状态与后端同步。
- 表单提交前执行统一的标识符校验（正则 `/^[a-zA-Z0-9_-]{2,50}$/`），并在 UI 上实时反馈错误。
- 使用 `AlertDialog` 组件处理删除等高风险操作，提供明确的二次确认提示。
- 在编辑模式下，若检测到破坏性变更（如删除参数或分支），通过 `detectBehaviorImpact` 触发包含影响范围的二次确认流程。
- 列表页采用防抖搜索（`useDebouncedValue`，延迟 300ms）以减少不必要的 API 请求。