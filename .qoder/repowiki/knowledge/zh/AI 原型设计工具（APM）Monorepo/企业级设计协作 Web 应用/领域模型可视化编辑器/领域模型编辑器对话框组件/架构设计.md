- 采用按操作类型（Create/Edit/Delete）与资源类型（Entity/Relation/Field/Boundary）拆分的扁平化组件结构，每个文件对应一个独立 Dialog。
- 依赖 `@/contexts/DomainModelContext` 统一获取数据与执行 CRUD 操作，实现 UI 与业务逻辑解耦。
- 复用 `@/components/ui` 下的基础 UI 组件（Dialog, AlertDialog, Input, Select 等）构建交互界面。
- 区分普通表单对话框（使用 `Dialog`）与危险操作确认对话框（使用 `AlertDialog`），以提供不同的用户体验与安全提示。